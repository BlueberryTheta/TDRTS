Background image

Place your map texture image here as:
  background.png

Notes
- Any PNG/JPG works; 1024×1024 or larger recommended.
- The renderer scales the image to canvas size (grid width × tile size).
- If the file is missing or fails to load, the game falls back to assets/map.svg.

